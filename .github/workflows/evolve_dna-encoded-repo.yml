name: Evolve DNA-Encoded Repo

on:
  schedule:
    - cron: '0 */6 * * *' # Run every 6 hours
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run evolution script
        run: |
          cd projects/tier10-biological/dna-encoded-repo
          python3 evolve.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Evolve DNA-Encoded Repo - Gen ${ github.run_number }"
          branch: "evolution/dna-encoded-repo"
          title: "Evolution: DNA-Encoded Repo - Day ${ github.run_number }"
          body: "Autonomous evolution step for DNA-Encoded Repo. Generation updated based on mathematical rules."
          delete-branch: true

      - name: Report Failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Incident: Evolution Halted for DNA-Encoded Repo',
              body: 'The daily evolution script for DNA-Encoded Repo failed. Please check the [Actions log](' + context.payload.repository.html_url + '/actions/runs/' + context.runId + ') for details.'
            })
