name: Evolve Graph Coloring Repo

on:
  schedule:
    - cron: '0 */6 * * *' # Run every 6 hours
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run evolution script
        run: |
          cd projects/tier8-mathematical/graph-coloring-repo
          python3 evolve.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Evolve Graph Coloring Repo - Gen ${ github.run_number }"
          branch: "evolution/graph-coloring-repo"
          title: "Evolution: Graph Coloring Repo - Day ${ github.run_number }"
          body: "Autonomous evolution step for Graph Coloring Repo. Generation updated based on mathematical rules."
          delete-branch: true

      - name: Report Failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Incident: Evolution Halted for Graph Coloring Repo',
              body: 'The daily evolution script for Graph Coloring Repo failed. Please check the [Actions log](' + context.payload.repository.html_url + '/actions/runs/' + context.runId + ') for details.'
            })
