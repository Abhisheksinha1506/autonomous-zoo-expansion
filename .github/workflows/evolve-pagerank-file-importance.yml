name: Evolve PageRank File Importance

on:
  schedule:
    - cron: '0 */6 * * *'  # Run every 6 hours
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run evolution script
        working-directory: ./projects/tier11-graph/pagerank-file-importance
        run: python3 evolve.py

      - name: Create Pull Request
        if: success()
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Evolve PageRank File Importance - Gen ${{ github.run_number }}"
          branch: "evolution/pagerank-file-importance"
          title: "PageRank File Importance Evolution: Gen ${{ github.run_number }}"
          body: |
            🧬 **Autonomous Evolution Update**
            
            Project: PageRank File Importance
            Generation: ${{ github.run_number }}
            
            This PR contains the latest evolution step generated automatically by GitHub Actions.
          delete-branch: true

      - name: Report Failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Evolution Failed: PageRank File Importance',
              body: 'The evolution script for PageRank File Importance failed. Check the [Actions log](' + context.payload.repository.html_url + '/actions/runs/' + context.runId + ') for details.',
              labels: ['evolution-failure', 'tier11-graph']
            })
