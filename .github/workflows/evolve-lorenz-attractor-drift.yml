name: Evolve Lorenz Attractor Drift

on:
  schedule:
    - cron: '0 */6 * * *'  # Run every 6 hours
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run evolution script
        working-directory: ./projects/tier9-physics/lorenz-attractor-drift
        run: python3 evolve.py

      - name: Create Pull Request
        if: success()
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Evolve Lorenz Attractor Drift - Gen ${{ github.run_number }}"
          branch: "evolution/lorenz-attractor-drift"
          title: "Lorenz Attractor Drift Evolution: Gen ${{ github.run_number }}"
          body: |
            🧬 **Autonomous Evolution Update**
            
            Project: Lorenz Attractor Drift
            Generation: ${{ github.run_number }}
            
            This PR contains the latest evolution step generated automatically by GitHub Actions.
          delete-branch: true

      - name: Report Failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Evolution Failed: Lorenz Attractor Drift',
              body: 'The evolution script for Lorenz Attractor Drift failed. Check the [Actions log](' + context.payload.repository.html_url + '/actions/runs/' + context.runId + ') for details.',
              labels: ['evolution-failure', 'tier9-physics']
            })
