name: Evolve Fibonacci File Growth

on:
  schedule:
    - cron: '0 */6 * * *'  # Run every 6 hours
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run evolution script
        working-directory: ./projects/tier8-mathematical/fibonacci-file-growth
        run: python3 evolve.py

      - name: Create Pull Request
        if: success()
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Evolve Fibonacci File Growth - Gen ${{ github.run_number }}"
          branch: "evolution/fibonacci-file-growth"
          title: "Fibonacci File Growth Evolution: Gen ${{ github.run_number }}"
          body: |
            🧬 **Autonomous Evolution Update**
            
            Project: Fibonacci File Growth
            Generation: ${{ github.run_number }}
            
            This PR contains the latest evolution step generated automatically by GitHub Actions.
          delete-branch: true

      - name: Report Failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Evolution Failed: Fibonacci File Growth',
              body: 'The evolution script for Fibonacci File Growth failed. Check the [Actions log](' + context.payload.repository.html_url + '/actions/runs/' + context.runId + ') for details.',
              labels: ['evolution-failure', 'tier8-mathematical']
            })
