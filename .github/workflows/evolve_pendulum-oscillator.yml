name: Evolve Pendulum Repo Oscillator

on:
  schedule:
    - cron: '0 */6 * * *' # Run every 6 hours
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run evolution script
        run: |
          cd projects/tier9-physics/pendulum-oscillator
          python3 evolve.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Evolve Pendulum Repo Oscillator - Gen ${ github.run_number }"
          branch: "evolution/pendulum-oscillator"
          title: "Evolution: Pendulum Repo Oscillator - Day ${ github.run_number }"
          body: "Autonomous evolution step for Pendulum Repo Oscillator. Generation updated based on mathematical rules."
          delete-branch: true

      - name: Report Failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Incident: Evolution Halted for Pendulum Repo Oscillator',
              body: 'The daily evolution script for Pendulum Repo Oscillator failed. Please check the [Actions log](' + context.payload.repository.html_url + '/actions/runs/' + context.runId + ') for details.'
            })
